= form_for(resource, :as => resource_name, :html => {:class => 'form-stacked'}, :url => registration_path(resource_name)) do |f|
  = devise_error_messages!
  %fieldset
    %legend Sign up
    %p
      = f.label :email
      = f.email_field :email
    %p
      = f.label :password
      = f.password_field :password
    %p
      = f.label :password_confirmation
      = f.password_field :password_confirmation
  %p= f.submit "Sign up"
= render :partial => "devise/shared/links"
